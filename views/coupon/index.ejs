<!DOCTYPE html>
<html lang="ko" class="h-100">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
    <style>
    </style>
</head>

<body class="d-flex flex-column h-100">
    <%include ../header %>

        <!-- Begin page content -->
        <main role="main" class="flex-shrink-0">
            <div class="container">
                <h1 class="mt-5">쿠폰함</h1>
                <div class="card mb-4 shadow-sm">
                    <div class="card-header">
                        <h4 class="my-0 font-weight-normal">등록</h4>
                    </div>
                    <div class="card-body">
                        <form action="/coupon" method="post">
                            <div class="form-group">
                                <label for="code_input">코드 입력</label>
                                <input type="text" class="form-control" id="code_input" name="cou_num">
                            </div>
                            <button type="submit" class="btn btn-primary">등록</button>
                        </form>
                    </div>
                </div>
                <%if(result.length != 0) {%>
                    <table>
                        <%for(var i=0; i<result.length; i++){%>
                            <div class="card my-4" data-num="<%=result[i].num%>">
                                <div class="card-body">
                                    <h5 class="card-title"><%=result[i].title%></h5>
                                    <p class="card-text"><%=result[i].description%></p>
                                    <%if(result[i].expire_date != undefined) {%>
                                        <p class="card-text">유효기간 : <%=result[i].expire_date%>까지</p>
                                    <%}%>
                                    <a href="coupon/<%=result[i].num%>" class="btn btn-primary">바코드 보이기</a>
                                </div>
                            </div>
                        <%}%>
                    </table>
                <%} else {%>
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">안내</h5>
                            <p class="card-text">등록되어 있는 쿠폰이 없네요. 위쪽 칸에서 추가시킬 수 있어요. 또한 이벤트를 통해 얻을 수 있어요</p>
                        </div>
                    </div>
                <%}%>
                <script>
                
                </script>
            </div>
        </main>

        <%include ../footer %>

</body>

</html>
